# Test-Harness Reliability Findings (overnight 2026-06-03)

Discovered while dogfooding the gate to ship the report-evidence-data-closure slice.
These ARE the "门禁不可靠" the user's overnight goal targets. S4 input.

| # | Finding | Where | Severity | Fix idea |
|---|---------|-------|----------|----------|
| F1 | `clarify --start` crashes on a slice opened with `next-step --mode strict`: §212 writes `clarify-locked-<step>.json` schema_version:2 (mode artifact), §128 clarify reads same file expecting schema_version:1 (understanding/...) → `writeClarifyLocked: required field "understanding" missing` | cli/src/clarify.ts:307 + clarify-artifact-schema.ts; §212/§128 share one filename, incompatible schemas | high (blocks R0 for strict slices) | split filenames OR merge schemas OR clarify reads mode-artifact gracefully |
| F2 ✅FIXED | `task complexity-judge` default mode=llm requires a provider; no offline default for the R0 flow → errors unless `--mode heuristic` | cli/src/commands.ts complexity-judge | med | default to heuristic when no provider, or document |
| F3 | Flaky gate test: `npm test --prefix cli` fails ~consistently (2514/2515) ONLY under the full gate's concurrent 5-suite run (cli-e2e spawns real terminals/daemons mutating shared `.dual-agent`); passes 2515/2515 in isolation every time | runGate full strategy + a non-isolated cli test reading real .dual-agent | high (blocks every submit; false NEEDS_WORK signal) | isolate the offending cli test's state dir; or serialize cli vs cli-e2e in the gate |
| F4 ✅FIXED | §151 visual-evidence fires on PLAN-phase [FIX] of a web/html slice even though no UI exists yet → false block, needs RL_VISUAL_EVIDENCE_OFF=1 opt-out | cli/src/policy.ts §151 | low | exempt PLAN-phase / pre-impl rounds |
| F5 | Submitting a one-line PLAN-body fix required clearing 6 sequential policy gates (test-plan marker, ## Test Results heading, file:line, §151, new-tests justification keyword, §137 log-cite freshness) — high ceremony friction for no-code rounds | policy.ts PLAN/FIX checks | med | a "doc/plan-only" round class that relaxes code-oriented checks |
| F7 | §201/§137 Test-Results claim-parser false-positives repeatedly block legitimate, *passing* [CODE]/[FIX]/[CONSENSUS] rounds. ANY backticked token near a numeric/exit context is treated as a test-command claim and checked against `test-execution-log.jsonl` — including markdown **module paths** (`` `submit_watcher::tests` ``), prose, and commands that don't *exactly* string-match a logged entry (logged `cargo test … --lib submit_watcher`, body cited `…submit_watcher::tests` → "unverified claims"). The `cmd: currentCmd \|\| '?'` sentinel also fails any numeric claim with no resolvable backticked cmd. Net effect: authors must mangle real prose / re-run via `RL_GATE_COMMANDS … gate` and exact-match the string, several wasted submit attempts per round. | cli/src/test-results-claim-verifier.ts:31/54/85 (parseTestResultClaims), cli/src/policy.ts:341/348 | high (blocks real passing rounds; pure author friction, catches no real fabrication here) | only treat a backticked token as a claim if it parses as a shell command (leading known runner: `cargo`/`npm`/`pytest`/`go`/`bash`/`ralph-lisa`…); match logged cmd by normalized prefix/substring, not exact string; never attach a numeric claim to a non-command token |
| F8 | Gate friction (F5+F7 + the §128/§123 ceremony) has a **user-visible production cost**, not just author friction. Test-user fugur's run (trivial single-file `login.html` task) hit `submit-ralph` blocked **16×** on policy gates (Test Results / plan-keeper / doc-oracle-spec). Because the rll-term submit-watcher only re-nudged the Lisa pane after a *successful* submit, the interactive Lisa pane was never woken again → the loop advanced only via headless `run-lisa` → "history.md shows PASS but the Lisa pane stayed blank". rll-term side fixed (wezterm `f6a8573a5`: pane wake-up decoupled from submit success), but the **super-rll trigger** — over-aggressive gates on a trivial task that should be `simple`/`standard` — is the root cause. Evidence: `tests/rll-term.zip` (logs/submit-watcher.log, command-events.jsonl). | policy.ts gate stack applied to simple/standard tasks; interacts with rll-term submit-watcher | high (silently routes the loop around the visible panes → user distrust of every PASS) | a `simple`/single-file-deliverable round class relaxing code-oriented ceremony (extends F5); plus F7 fix. NOTE: the `[PLAN]`→`[TDD-PLAN]` split (gate-free `[PLAN]`) in HEAD already addresses part of this, but the **installed `ralph-lisa` 0.12.0** that wezterm/fugur run predates it — the fix only reaches users on the next CLI publish. |

These feed S4 (全 gate 类型接入 + 替换弱 builder) — fixing them is part of "确保通过门禁的应用真符合质量要求".

## Status (2026-06-04 overnight follow-up)
- F2 ✅ fixed (commit 31e767d) — heuristic fallback.
- F4 ✅ fixed (commit fc43895) — pre-impl-round downgrade.
- F6 ⏸ DEFERRED — the obvious fix (skip exit-only `?` claims) weakens deliberate fake-exit-claim detection (C3.a/C2.h). Needs careful verify-by-exit design.
- F1 (clarify §212/§128 file collision) — needs a file-rename refactor (the v2 mode-artifact and v1 clarify-artifact genuinely cannot share clarify-locked-<step>.json). Use a separate filename for §128.
- F3 (load-flaky cli test) — must first reproduce which cli test fails under the concurrent full-gate run (passes solo); likely a non-isolated test reading the real .dual-agent while cli-e2e mutates it.
- F5 (6-gate friction on no-code rounds) — a policy design change: a 'doc/plan-only' round class relaxing code-oriented checks.

## Status (2026-06-06 — reported from the wezterm/rll-term loop, installed ralph-lisa 0.12.0)
- **F7 NEW** (§201 claim-parser false-positives) — reproduced repeatedly this session: a [CODE] round whose tests genuinely passed (709/709) was blocked twice, first on `file:line` and then because the backticked module path `submit_watcher::tests` (in a prose sentence and a PLAN-table Command cell) was read as an unverified test-command claim. Earlier the same session a [FIX] was blocked because a backticked `ralph-lisa task complexity-verify …` in the Test-Results section had no matching execution-log entry. Confirms the long-standing `rll-201-claim-parser-false-positive` note. Fix idea in table (command-shaped tokens only + prefix/substring log match). Until fixed, the working-around recipe is: run each cited cmd via `RL_GATE_COMMANDS="<cmd>" ralph-lisa quality-gate` first, then cite the EXACT logged string; keep module paths / prose out of backticks in the Test Results section.
- **F8 NEW** (gate friction → blank-pane desync) — root-caused from test-user fugur's `tests/rll-term.zip`. The rll-term half is already fixed in wezterm `f6a8573a5`; this finding tracks the super-rll half (relax ceremony for trivial single-file tasks so simple work doesn't get gate-blocked into the headless-run-lisa fallback path). Partly mitigated by the HEAD `[PLAN]`/`[TDD-PLAN]` split, but not in the published 0.12.0 CLI users run.
- Cross-ref: this batch was reported by the rll-term Ralph after shipping the `watcher-nudge-decoupled-from-submit-success` slice; F8's rll-term-side fix and F7's workaround were both exercised live.
